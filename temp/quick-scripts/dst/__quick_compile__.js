
(function () {
var scripts = [{"deps":{"./assets/Script/network/NetNode":2,"./assets/Script/common/TaskQueue":6,"./assets/Script/network/NetInterface":13,"./assets/Script/network/WebSock":14,"./assets/Script/example/EmptyScene":15,"./assets/Script/network/NetManager":18,"./assets/Script/res/ResPool":20,"./assets/Script/res/NodePool":22,"./assets/Script/common/EventManager":24,"./assets/Script/ui/UIManager":1,"./assets/Script/res/ResManager":4,"./assets/Script/example/uiviews/UIHall":3,"./assets/Script/example/NetExample":5,"./assets/Script/example/UIExample":7,"./assets/Script/example/ResExample":8,"./assets/Script/example/ResKeeperExample":9,"./assets/Script/res/ResLoader":11,"./assets/Script/res/ResKeeper":16,"./assets/Script/res/ResLeakChecker":17,"./assets/Script/res/ResUtil":19,"./assets/Script/ui/UIView":21,"./assets/Script/example/uiviews/UILogin":10,"./assets/Script/example/uiviews/UIBag":12,"./assets/Script/example/uiviews/UINotice":23},"path":"preview-scripts/__qc_index__.js"},{"deps":{"../res/ResLoader":11,"./UIView":21},"path":"preview-scripts/assets/Script/ui/UIManager.js"},{"deps":{},"path":"preview-scripts/assets/Script/network/NetNode.js"},{"deps":{"../../ui/UIView":21,"../../ui/UIManager":1,"../UIExample":7},"path":"preview-scripts/assets/Script/example/uiviews/UIHall.js"},{"deps":{"./ResKeeper":16,"./ResUtil":19},"path":"preview-scripts/assets/Script/res/ResManager.js"},{"deps":{"../network/WebSock":14,"../network/NetManager":18,"../network/NetNode":2,"../network/NetInterface":13},"path":"preview-scripts/assets/Script/example/NetExample.js"},{"deps":{},"path":"preview-scripts/assets/Script/common/TaskQueue.js"},{"deps":{"../ui/UIManager":1},"path":"preview-scripts/assets/Script/example/UIExample.js"},{"deps":{"../res/ResLoader":11},"path":"preview-scripts/assets/Script/example/ResExample.js"},{"deps":{"../res/ResLeakChecker":17,"../res/ResLoader":11,"../res/ResUtil":19},"path":"preview-scripts/assets/Script/example/ResKeeperExample.js"},{"deps":{"../../ui/UIView":21,"../../ui/UIManager":1,"../UIExample":7},"path":"preview-scripts/assets/Script/example/uiviews/UILogin.js"},{"deps":{"./ResManager":4},"path":"preview-scripts/assets/Script/res/ResLoader.js"},{"deps":{"../../ui/UIView":21,"../../ui/UIManager":1},"path":"preview-scripts/assets/Script/example/uiviews/UIBag.js"},{"deps":{},"path":"preview-scripts/assets/Script/network/NetInterface.js"},{"deps":{},"path":"preview-scripts/assets/Script/network/WebSock.js"},{"deps":{},"path":"preview-scripts/assets/Script/example/EmptyScene.js"},{"deps":{"./ResLoader":11},"path":"preview-scripts/assets/Script/res/ResKeeper.js"},{"deps":{"./ResUtil":19},"path":"preview-scripts/assets/Script/res/ResLeakChecker.js"},{"deps":{},"path":"preview-scripts/assets/Script/network/NetManager.js"},{"deps":{"./ResKeeper":16,"./ResManager":4},"path":"preview-scripts/assets/Script/res/ResUtil.js"},{"deps":{},"path":"preview-scripts/assets/Script/res/ResPool.js"},{"deps":{"../res/ResKeeper":16},"path":"preview-scripts/assets/Script/ui/UIView.js"},{"deps":{},"path":"preview-scripts/assets/Script/res/NodePool.js"},{"deps":{"../../ui/UIView":21},"path":"preview-scripts/assets/Script/example/uiviews/UINotice.js"},{"deps":{},"path":"preview-scripts/assets/Script/common/EventManager.js"}];
var entries = ["preview-scripts/__qc_index__.js"];
var bundleScript = 'preview-scripts/__qc_bundle__.js';

/**
 * Notice: This file can not use ES6 (for IE 11)
 */
var modules = {};
var name2path = {};

// Will generated by module.js plugin
// var scripts = ${scripts};
// var entries = ${entries};
// var bundleScript = ${bundleScript};

if (typeof global === 'undefined') {
    window.global = window;
}

var isJSB = typeof jsb !== 'undefined';

function getXMLHttpRequest () {
    return window.XMLHttpRequest ? new window.XMLHttpRequest() : new ActiveXObject('MSXML2.XMLHTTP');
}

function downloadText(url, callback) {
    if (isJSB) {
        var result = jsb.fileUtils.getStringFromFile(url);
        callback(null, result);
        return;
    }

    var xhr = getXMLHttpRequest(),
        errInfo = 'Load text file failed: ' + url;
    xhr.open('GET', url, true);
    if (xhr.overrideMimeType) xhr.overrideMimeType('text\/plain; charset=utf-8');
    xhr.onload = function () {
        if (xhr.readyState === 4) {
            if (xhr.status === 200 || xhr.status === 0) {
                callback(null, xhr.responseText);
            }
            else {
                callback({status:xhr.status, errorMessage:errInfo + ', status: ' + xhr.status});
            }
        }
        else {
            callback({status:xhr.status, errorMessage:errInfo + '(wrong readyState)'});
        }
    };
    xhr.onerror = function(){
        callback({status:xhr.status, errorMessage:errInfo + '(error)'});
    };
    xhr.ontimeout = function(){
        callback({status:xhr.status, errorMessage:errInfo + '(time out)'});
    };
    xhr.send(null);
};

function loadScript (src, cb) {
    if (typeof require !== 'undefined') {
        require(src);
        return cb();
    }

    // var timer = 'load ' + src;
    // console.time(timer);

    var scriptElement = document.createElement('script');

    function done() {
        // console.timeEnd(timer);
        // deallocation immediate whatever
        scriptElement.remove();
    }

    scriptElement.onload = function () {
        done();
        cb();
    };
    scriptElement.onerror = function () {
        done();
        var error = 'Failed to load ' + src;
        console.error(error);
        cb(new Error(error));
    };
    scriptElement.setAttribute('type','text/javascript');
    scriptElement.setAttribute('charset', 'utf-8');
    scriptElement.setAttribute('src', src);

    document.head.appendChild(scriptElement);
}

function loadScripts (srcs, cb) {
    var n = srcs.length;

    srcs.forEach(function (src) {
        loadScript(src, function () {
            n--;
            if (n === 0) {
                cb();
            }
        });
    })
}

function formatPath (path) {
    let destPath = window.__quick_compile_project__.destPath;
    if (destPath) {
        let prefix = 'preview-scripts';
        if (destPath[destPath.length - 1] === '/') {
            prefix += '/';
        }
        path = path.replace(prefix, destPath);
    }
    return path;
}

window.__quick_compile_project__ = {
    destPath: '',

    registerModule: function (path, module) {
        path = formatPath(path);
        modules[path].module = module;
    },

    registerModuleFunc: function (path, func) {
        path = formatPath(path);
        modules[path].func = func;

        var sections = path.split('/');
        var name = sections[sections.length - 1];
        name = name.replace(/\.(?:js|ts|json)$/i, '');
        name2path[name] = path;
    },

    require: function (request, path) {
        var m, requestScript;

        path = formatPath(path);
        if (path) {
            m = modules[path];
            if (!m) {
                console.warn('Can not find module for path : ' + path);
                return null;
            }
        }

        if (m) {
            requestScript = scripts[ m.deps[request] ];
        }
        
        path = '';
        if (!requestScript) {
            // search from name2path when request is a dynamic module name
            if (/^[\w- .]*$/.test(request)) {
                path = name2path[request];
            }

            if (!path) {
                if (CC_JSB) {
                    return require(request);
                }
                else {
                    console.warn('Can not find deps [' + request + '] for path : ' + path);
                    return null;
                }
            }
        }
        else {
            path = formatPath(requestScript.path);
        }

        m = modules[path];
        
        if (!m) {
            console.warn('Can not find module for path : ' + path);
            return null;
        }

        if (!m.module && m.func) {
            m.func();
        }

        if (!m.module) {
            console.warn('Can not find module.module for path : ' + path);
            return null;
        }

        return m.module.exports;
    },

    run: function () {
        entries.forEach(function (entry) {
            entry = formatPath(entry);
            var module = modules[entry];
            if (!module.module) {
                module.func();
            }
        });
    },

    load: function (cb) {
        var self = this;

        var srcs = scripts.map(function (script) {
            var path = formatPath(script.path);
            modules[path] = script;

            if (script.mtime) {
                path += ("?mtime=" + script.mtime);
            }
            return path;
        });

        console.time && console.time('load __quick_compile_project__');
        // jsb can not analysis sourcemap, so keep separate files.
        if (bundleScript && !isJSB) {
            downloadText(formatPath(bundleScript), function (err, bundleSource) {
                console.timeEnd && console.timeEnd('load __quick_compile_project__');
                if (err) {
                    console.error(err);
                    return;
                }
                console.time && console.time('eval __quick_compile_project__');
                var sources = bundleSource.split('\n//------QC-SOURCE-SPLIT------\n');
                for (var i = 0; i < sources.length; i++) {
                    if (sources[i]) {
                        window.eval(sources[i]);
                        // not sure why new Function cannot set breakpoints precisely
                        // new Function(sources[i])()
                    }
                }
                self.run();
                console.timeEnd && console.timeEnd('eval __quick_compile_project__');
                cb();
            })
        }
        else {
            loadScripts(srcs, function () {
                self.run();
                console.timeEnd && console.timeEnd('load __quick_compile_project__');
                cb();
            });
        }
    }
};

// Polyfill for IE 11
if (!('remove' in Element.prototype)) {
    Element.prototype.remove = function () {
        if (this.parentNode) {
            this.parentNode.removeChild(this);
        }
    };
}
})();
    